$("#post_<%= @post.id %> .favorite-btn").html("<%= j(render 'favorites/favorite-btn', post: @post) %>");
$("#favorite-users").html("<%= j(render "favorites/favorite-users", favorites: @favorites) %>");
