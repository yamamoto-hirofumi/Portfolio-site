<div class="contianer">
  <h2 class="text-center p-3">フォロー中</h2>
  <% if @users.exists? %>
    <%= render "users/index", users: @users %>
  <% else %>
    <p class="text-center">フォローしているユーザーはいません</p>
  <% end %>
</div>